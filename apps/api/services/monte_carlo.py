async def run_sec_simulation(job_id: str, params: dict) -> dict:
    """Week 4 scope — Monte Carlo SEC simulation engine.

    Returns p50/p70/p95 completion date predictions via Vertex AI.
    """
    raise NotImplementedError("Monte Carlo SEC simulation is Week 4 scope")
