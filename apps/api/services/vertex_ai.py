from config import get_settings


async def call_gemini(prompt: str, job_id: str) -> dict:
    """Week 4 scope — Vertex AI / Gemini integration.

    Every call MUST log vertex_ai_request_id to simulation_runs for hackathon evidence.
    """
    raise NotImplementedError("Vertex AI integration is Week 4 scope")
